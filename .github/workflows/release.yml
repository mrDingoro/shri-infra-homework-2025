name: 3. 🏗️ Создание релиза (ручной запуск)

on:
  workflow_dispatch:
    inputs:
      release_number:
        description: 'Номер релиза (оставьте пустым для автоматического использования github.run_number)'
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Получить ветку релиза
      uses: actions/checkout@v4

    - name: Настроить Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Установить зависимости
      run: npm ci

    - name: Запустить линтер
      run: npm run lint

  test:
    runs-on: ubuntu-latest

    steps:
    - name: Получить ветку релиза
      uses: actions/checkout@v4

    - name: Настроить Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Установить зависимости
      run: npm ci

    - name: Запустить тесты
      run: npm test

  typecheck:
    runs-on: ubuntu-latest

    steps:
    - name: Получить ветку релиза
      uses: actions/checkout@v4

    - name: Настроить Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Установить зависимости
      run: npm ci

    - name: Проверить типы
      run: npx tsc --noEmit

  release:
    needs: [lint, test, typecheck]
    runs-on: ubuntu-latest

    steps:
    - name: Получить ветку релиза
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Настроить Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Установить зависимости
      run: npm ci

    - name: Собрать проект
      run: npm run build

    - name: Создать ветку релиза
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        RELEASE_NUMBER="${{ github.event.inputs.release_number || github.run_number }}"
        git checkout -b releases/$RELEASE_NUMBER
        git push origin releases/$RELEASE_NUMBER

    - name: Авторизация в Yandex Container Registry
      uses: docker/login-action@v3
      with:
        registry: cr.yandex
        username: json_key
        password: ${{ secrets.YC_SA_JSON_CREDENTIALS }}

    - name: Настроить Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Собрать и загрузить Docker образ
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          cr.yandex/${{ secrets.YC_REGISTRY_ID }}/app:${{ github.event.inputs.release_number || github.run_number }}
          cr.yandex/${{ secrets.YC_REGISTRY_ID }}/app:${{ github.event.inputs.release_number || github.run_number }}_latest
        platforms: linux/amd64

    - name: Создать Git тег
      run: |
        RELEASE_NUMBER="${{ github.event.inputs.release_number || github.run_number }}"
        git tag $RELEASE_NUMBER
        git push origin $RELEASE_NUMBER

    - name: Получить предыдущий тег релиза
      id: prev_tag
      run: |
        PREV_TAG=$(git tag --sort=-version:refname | grep -E '^[0-9]+$' | head -2 | tail -1)
        if [ -z "$PREV_TAG" ]; then
          PREV_TAG=$(git rev-list --max-parents=0 HEAD)
        fi
        echo "prev_tag=$PREV_TAG" >> $GITHUB_OUTPUT

    - name: Получить коммиты с последнего релиза
      id: commits
      run: |
        COMMITS=$(git log --oneline ${{ steps.prev_tag.outputs.prev_tag }}..${{ github.sha }} --pretty=format:"- %s (%h)")
        echo "commits<<EOF" >> $GITHUB_OUTPUT
        echo "$COMMITS" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

    - name: Создать GitHub Issue
      uses: actions/github-script@v7
      with:
        script: |
          const releaseNumber = '${{ github.event.inputs.release_number || github.run_number }}';
          const { data: issue } = await github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: `Релиз ${releaseNumber}`,
            body: `## Релиз ${releaseNumber}

            **Дата:** ${new Date().toLocaleString('ru-RU')}
            **Автор релиза:** @${{ github.actor }}
            **Версия:** ${releaseNumber}

            ### Коммиты:
            ${{ steps.commits.outputs.commits }}

            ### Docker образ:
            \`cr.yandex/${{ secrets.YC_REGISTRY_ID }}/app:${releaseNumber}\`

            ### Статус:
            ✅ Релиз создан и готов к деплою
            `,
            labels: ['release', 'deployment']
          });

          console.log('Создан issue #' + issue.number);

    - name: Обновить CHANGELOG.md
      run: |
        RELEASE_NUMBER="${{ github.event.inputs.release_number || github.run_number }}"
        echo "# Changelog" > temp_changelog.md
        echo "" >> temp_changelog.md
        echo "## [$RELEASE_NUMBER] - $(date +'%Y-%m-%d')" >> temp_changelog.md
        echo "" >> temp_changelog.md
        echo "### Изменения:" >> temp_changelog.md
        echo "${{ steps.commits.outputs.commits }}" >> temp_changelog.md
        echo "" >> temp_changelog.md

        # Проверяем, существует ли CHANGELOG.md и не пустой ли он
        if [ -f CHANGELOG.md ] && [ -s CHANGELOG.md ]; then
          # Файл существует и не пустой
          # Проверяем, есть ли уже релизы (строки начинающиеся с ##)
          if grep -q "^## \[" CHANGELOG.md; then
            # Есть предыдущие релизы - добавляем их после нового релиза
            tail -n +2 CHANGELOG.md >> temp_changelog.md
          else
            # Это базовый CHANGELOG без релизов - добавляем описание после нового релиза
            # Пропускаем заголовок и добавляем остальное
            tail -n +2 CHANGELOG.md >> temp_changelog.md
          fi
        else
          # Файл не существует или пустой - добавляем комментарий
          echo "<!-- Предыдущие релизы будут добавлены здесь -->" >> temp_changelog.md
        fi

        mv temp_changelog.md CHANGELOG.md

        git add CHANGELOG.md
        git commit -m "Обновить CHANGELOG.md для релиза $RELEASE_NUMBER"
        git push origin releases/$RELEASE_NUMBER

    - name: 📝 Создать PR для обновления CHANGELOG.md в main
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        RELEASE_NUMBER="${{ github.event.inputs.release_number || github.run_number }}"
        # Создать новую ветку для обновления CHANGELOG от main
        BRANCH_NAME="update-changelog-release-$RELEASE_NUMBER"
        git checkout main
        git checkout -b $BRANCH_NAME

        # Скопировать CHANGELOG.md из релизной ветки
        git show releases/$RELEASE_NUMBER:CHANGELOG.md > new_changelog.md

        # Отладочная информация
        echo "=== Отладка CHANGELOG.md ==="
        echo "Текущая ветка: $(git branch --show-current)"
        echo "Последний коммит: $(git log -1 --oneline)"
        echo "Размер текущего CHANGELOG.md: $(wc -c < CHANGELOG.md 2>/dev/null || echo 0)"
        echo "Размер нового CHANGELOG.md: $(wc -c < new_changelog.md)"
        echo "Содержимое текущего CHANGELOG.md:"
        cat CHANGELOG.md || echo "(файл не существует)"
        echo "Содержимое нового CHANGELOG.md:"
        cat new_changelog.md
        echo "=== Конец отладки ==="

        # Проверить есть ли изменения между текущим и новым CHANGELOG.md
        if [ -f CHANGELOG.md ] && [ -s CHANGELOG.md ]; then
          # Файл существует и не пустой
          # Проверяем, есть ли в текущем CHANGELOG.md релизы (строки начинающиеся с "## [")
          if grep -q "^## \[" CHANGELOG.md; then
            # Есть релизы - сравниваем файлы
            if diff -q CHANGELOG.md new_changelog.md > /dev/null; then
              echo "CHANGELOG.md не изменился, PR не нужен"
              rm new_changelog.md
              exit 0
            else
              echo "CHANGELOG.md изменился, создаем PR"
            fi
          else
            # Нет релизов (только базовое описание) - всегда создаем PR
            echo "CHANGELOG.md не содержит релизов, создаем PR"
          fi
        else
          # Файл не существует или пустой - всегда создаем PR
          echo "CHANGELOG.md не существует или пустой в main, создаем PR"
        fi

        # Заменить CHANGELOG.md новым содержимым
        mv new_changelog.md CHANGELOG.md

        # Закоммитить изменения
        git add CHANGELOG.md
        git commit -m "Обновить CHANGELOG.md для релиза $RELEASE_NUMBER"
        git push origin $BRANCH_NAME

        # Создать Pull Request
        gh pr create \
          --title "📝 Обновить CHANGELOG.md для релиза $RELEASE_NUMBER" \
          --body "Автоматическое обновление CHANGELOG.md после создания релиза $RELEASE_NUMBER

        ### Изменения:
        - Добавлена запись о релизе $RELEASE_NUMBER
        - Указаны коммиты включенные в релиз
        - Добавлена дата релиза: $(date '+%Y-%m-%d')

        Этот PR создан автоматически workflow релиза." \
          --base main \
          --head $BRANCH_NAME \
          --label "release" \
          --label "changelog"
